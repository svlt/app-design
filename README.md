# app-design

*slvt/front design mockup*

## Installation

1. Clone the repo
2. `cd` to `assets/`
3. Run `bower install`

## Development

1. `cd` to `assets/`
2. Run `npm install`
3. Run `gulp scss` to compile the SCSS or just `gulp` to run continuously.
